set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -g mouse on
set escape-time 0
set -s set-clipboard external
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# DESIGN TWEAKS
# don't do anything when a 'bell' rings
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

# clock mode
setw -g clock-mode-colour colour4

# copy mode
setw -g mode-style 'fg=colour1 bg=colour18 bold'

# pane borders
set -g pane-border-style 'fg=colour7'
set -g pane-active-border-style 'fg=colour4'

# statusbar
set -g status-position bottom
set -g status-justify left
set -g status-style 'fg=colour6'
set -g status-left '#[fg=colour10,bold]#[bg=#3C3C3E,fg=colour15]  #S #[default]'
set -g status-right '#[bg=#3C3C3E,fg=colour15] %Y-%m-%d %H:%M '
set -g status-right '#[bg=#3C3C3E,fg=colour15]  %d/%m/%y %I:%M%p '
set -g status-right-length 50
set -g status-left-length 15

setw -g window-status-current-style 'fg=colour0 bg=colour12 bold'
setw -g window-status-current-format ' #I-#W#F '

setw -g window-status-style 'fg=colour6 dim'
setw -g window-status-format ' #[fg=colour15]#I#[fg=colour7]-#W#[fg=colour1]#F '

setw -g window-status-bell-style 'fg=colour2 bg=colour6 bold'

# messages
set -g message-style 'fg=colour2 bg=colour0 bold'
